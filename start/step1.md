You can start the interactive session by first installing Rasa.

```{execute}
python -m pip install rasa
```

Once Rasa is installed you can create a new Rasa project via.

```{execute}
python -m rasa init
```